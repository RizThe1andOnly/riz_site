/**
 * Functions that will be the onEvent actions for the up and 
 * down button. They will be used to change slides.
 */

// export function transitionUp(currentSlideIndex,elements){
//     let newSlideIndex = currentSlideIndex;
//     if(newSlideIndex != 0){
//         newSlideIndex = newSlideIndex - 1;
//     }
//     let focusElement = elements[newSlideIndex];
//     focusElement.scrollIntoView();
// }

// export function transitionDown(currentSlideIndex,maxSlides){
//     let newSlideIndex = currentSlideIndex;
//     if(newSlideIndex < maxSlides){
//         newSlideIndex = newSlideIndex + 1;
//     }
//     let focusElement = elements[newSlideIndex];
//     focusElement.scrollIntoView();  
// }

